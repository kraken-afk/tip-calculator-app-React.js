const Title = () => {
  const block = { display: "block" };
  return (
    <div className="title">
      <h1>
        <span style={block}>SPLI</span>
        <span style={block}>TTER</span>
      </h1>
    </div>
  );
};

export default Title;
